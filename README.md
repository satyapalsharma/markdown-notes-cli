# markdown-notes-cli
A lightweight Node.js command-line interface built with TypeScript for managing markdown notes. It allows users to quickly create new markdown files and list existing ones, storing all notes as .md files in a simple directory structure for easy access and organization.
